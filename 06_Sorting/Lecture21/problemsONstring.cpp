//[01] Majority Element.    LeetCode :-169
/*[02] Given an array with N distinct elements , converts the given array to a form
whare all elements are in the range from 0 to N-1.The order of the elements is the
same ,i.e., 0 is placed int the place of the smallest elements.*/ //:- type of the insertion sort
//[03] Assign Cookies.      LeetCode :-455
/*[04] Given an array , arr[] containing'n' integers , the task is to find the integer (say k)
such that after replacing each and every element of every index of the array by |ai - k|
where (i belongs from [1,n]), results in the sorted array. If no such integer exist that
satisfies the above conditions then return -1. Find the range of K .*/